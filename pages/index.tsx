import Head from 'next/head';

export const Home = (): JSX.Element => (
  <div className="container">
    <Head>
      <title>Loan (finance) calculator</title>
      <link rel="icon" href="/favicon.ico" />
    </Head>

    <main>
      <h1>Finance test</h1>
    </main>

  </div>
);

export default Home;
